{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.frontend.ingressName }}
  labels:
    app: shopverse
  annotations:
    {{- toYaml .Values.ingress.annotations | nindent 4 }}
spec:
  ingressClassName: {{ .Values.frontend.ingressClassName | default .Values.ingress.ingressClassName | quote }}
  rules:
    - host: {{ .Values.frontend.ingressHost | default "" | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.frontend.serviceName }}
                port:
                  number: {{ .Values.frontend.servicePort }}
  tls:
    - hosts:
        - {{ .Values.frontend.ingressHost | default "" | quote }}
      secretName: shopverse-frontend-tls
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.backend.ingressName }}
  labels:
    app: shopverse
  annotations:
    {{- toYaml .Values.ingress.annotations | nindent 4 }}
spec:
  ingressClassName: {{ .Values.backend.ingressClassName | default .Values.ingress.ingressClassName | quote }}
  rules:
    - host: {{ .Values.backend.ingressHost | default "" | quote }}
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.backend.serviceName }}
                port:
                  number: {{ .Values.backend.servicePort }}
          - path: /health
            pathType: Exact
            backend:
              service:
                name: {{ .Values.backend.serviceName }}
                port:
                  number: {{ .Values.backend.servicePort }}
  tls:
    - hosts:
        - {{ .Values.backend.ingressHost | default "" | quote }}
      secretName: shopverse-backend-tls
{{- end }}
